# CLAUDE.md

All project rules live in a single file so they can never drift between tools.
The rules below are imported verbatim and are **mandatory for Claude Code** — treat
every "MUST", "ALWAYS", "NEVER", and "NO EXCEPTIONS" in them as binding.

@.github/copilot-instructions.md
